# Next Work

Last updated: 2026-07-22

## Immediate

1. After the user signs in on the open port-5173 runner, reproduce the exact
   authenticated catalog `500` and capture its server-side cause.
2. If a correction is required, add a regression test before the scoped fix.
3. Resume visual QA for cards, detail, lightbox, filters/order and the 39
   `PRICE_PENDING` items; then evaluate NEXO-0036/NEXO-0033 closeout.

## Parallel Planned Work

- `NEXO-0031` deployment `9p8buojpD` is Ready with the same-origin API proxy.
  The reversible local restore and authenticated local API/media checks pass.
  Complete the equivalent hosted acceptance, then continue the remaining DNS,
  automated-backup, and infrastructure close gates.
- `NEXO-0037` is active for sales readiness; follow its task row and plan
  without replacing the default visual-QA focus until explicitly selected.
- `NEXO-0039` is active for first-login password setup flow (P0).
- `NEXO-0040` (P0) — Fix navigation, public routes, and MobileNav completeness.
- `NEXO-0041` (P1) — Exchange Rate UI (current rate viewer).
- `NEXO-0042` (P2) — Photo upload button in inventory editor.
- `NEXO-0043` (P1) — Implement real Capture/Quick Purchase page.

## Agent Workflow Follow-Up

1. Restart OpenCode after closed `NEXO-0049` so manual Plannotator commands,
   attention, the Nexo footer, privacy guards, and telemetry load in the normal
   process.
2. Use `runbooks/NEXO-0049-opencode2-productivity-observability.md` for the
   doctor, local visual review, optional loopback status, and rollback.
3. Authorize GitHub OAuth or start the dedicated Chrome profile only when the
   corresponding read or browser-inspection task needs it.
4. `NEXO-0048` is closed; use its architecture/dependency skills and exact
   evaluation contracts when a future governed task declares those requirements.
5. ChatGPT/Codex is ready; open a new task in this repo to load Terra/High and
   compact startup defaults.
6. `NEXO-0044` repaired OpenCode and validated Sol, plugins, MCP discovery,
   planner permissions, and the compact context compiler. Restart OpenCode
   before evaluating behavior in a new session.
7. Run a paid Terra A/B benchmark only after separate approval. Compare cost
   per accepted result, all token classes, tool turns, and validation outcome.

## Product Sequence

- Resume `NEXO-0008` after the current visual QA/closeout work.
- Use `harness/control/tasks.md` for all other active, blocked, and planned
  work; this file intentionally avoids duplicating the full task table.
