# Journal - 2026-07-22

## NEXO-0031 Vercel Root Directory Remediation (nexo)

- The user supplied a Vercel build from commit `5e352d9` that ran `vite build`
  and failed with `vite: command not found` (exit 127).
- A clean checkout reproduced the exact failure from the repository root, which
  has no `package.json` and only an empty `package-lock.json`.
- A clean frozen-lockfile install and build under `front/` passed with Vite
  7.0.6, 2,563 transformed modules, and generated `dist`; the current frontend
  build and deployment-config assertions also pass.
- Decision: keep `front/vercel.json` as the only deployment config. Do not add a
  root manifest or duplicate Vite dependencies. Configure Vercel Root Directory
  as `front`, use Vite and Node 22.x, and remove inherited build/output/install
  overrides.
- Updated `RUNBOOK-NEXO-0031-operations.md` with exact settings, explicit
  authorization before external mutation, expected log evidence,
  troubleshooting, and rollback.
- Scoped QA passed local remediation readiness. Initial security review found a
  low-severity authorization-order ambiguity; the runbook was corrected and
  security review 002 approved the scoped remediation. The exact pnpm version
  remains a pre-existing low reproducibility risk to capture in the hosted log.
- No secret, frontend source, dependency, lockfile, Vercel setting, browser,
  deployment, commit, push, or other external state was changed.
- Implementation:
  `implementations/IMPL-NEXO-0031-vercel-root-directory-remediation.md`.
- Report:
  `reports/2026-07-22/NEXO-0031-vercel-root-directory-remediation-session-002.md`.

## NEXO-0031 Vercel/ngrok Same-Origin Deployment (nexo)

- Reproduced the browser failure as `200 text/html` with
  `ngrok-error-code: ERR_NGROK_6024`; the same request with the ngrok bypass
  header reached the API and returned JSON.
- Confirmed the prior production bundle embedded the direct ngrok API URL and
  that the hosted `/api/v1` path fell through to `index.html`.
- After explicit user authorization, verified Vercel Project Settings use Root
  Directory `front`, Vite, Node 22.x, and no build/install/output overrides.
- Updated the Production and Preview variables to
  `VITE_API_BASE_URL=/api/v1` and the current operator-controlled
  `BACKEND_ORIGIN`, then redeployed commit `4cdb514` without the old build cache.
- Deployment `9p8buojpD` became Ready in 22 seconds and retained
  `nexo-app-blond.vercel.app`.
- Hosted `/api/v1/catalogs/stores` now returns backend
  `401 application/json` when unauthenticated, with no ngrok interstitial. The
  new bundle contains the relative API path and no direct ngrok URL.
- Authenticated login, catalog, and protected-photo acceptance remains pending;
  no user credentials were requested or inspected.
- Scoped deployment QA passed. Security validated the fixed upstream and absence
  of client hostname exposure, but blocks NEXO-0031 close pending explicit CSRF
  protection or risk acceptance and authenticated cookie/media verification.
