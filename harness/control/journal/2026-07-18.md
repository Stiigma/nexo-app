# Journal - 2026-07-18

## Password Setup Email Admin Trigger (nexo-build)

- Added admin ability to send password setup emails to users without passwords.
- Backend: new `POST /users/:id/send-password-setup` endpoint, `hasPassword` field on `UserResponseDto`.
- Frontend: `useSendPasswordSetup` hook, Mail button + yellow "Sin contraseña" badge on `UsersPage`.
- Both builds pass cleanly. Changes uncommitted per user request.
- Report: `reports/2026-07-18/password-setup-email-admin-session-001.md`

## NEXO-0044 OpenCode Effective Configuration (nexo-build)

- Repaired the active pnpm-installed OpenCode binary in place by running its
  bundled postinstall script; version remains `0.0.0-dev-202607181600`.
- Configured `openai/gpt-5.6-sol` with existing risk-tiered variants and low
  verbosity.
- Removed the invalid nested Graphify plugin path; effective config now loads
  the three real local plugins once each.
- Allowed `nexo-plan` to edit only `harness/control/**` while retaining denied
  shell access and limited delegation.
- Verification: 4/4 focused tests, 19/19 complete OpenCode harness tests,
  effective config and agent resolution, two connected MCPs, and compact
  context compilation all pass.
- Refreshed the Graphify AST graph; optional SQL parsing remains unavailable.
- No commit, push, deploy, paid inference, or credential change was performed.
- Report: `reports/2026-07-18/NEXO-0044-opencode-effective-config-session-001.md`.
- Closeout: `closeouts/NEXO-0044-opencode-effective-config.md`.

## NEXO-0045 Single-Chat Orchestrator (nexo-plan)

- Registered the next harness tracer bullet: one visible `nexo` agent with
  hidden specialist subagents and no second-level delegation.
- Selected OpenCode's native primary/subagent topology and the existing compact
  context compiler; no orchestration dependency or structured-state migration
  is needed for this slice.
- Prepared `plans/NEXO-0045-single-chat-orchestrator.md` and
  `handoffs/HOFF-2026-07-18-single-chat-orchestrator.md` for implementation.
- Implemented `nexo` as the only selectable Nexo primary and converted eight
  roles into hidden subagents with no Task permission.
- Routed all 12 Nexo commands through the orchestrator and aligned budget
  attribution with the root agent while retaining phase totals.
- Scoped non-implementation role writes and retained explicit confirmation
  gates for external actions.
- Verification: 4/4 focused tests, 23/23 complete harness tests, effective agent
  diagnostics, context compilation, startup, QA, security, and Graphify pass.
- No commit, push, deploy, external mutation, dependency change, credential
  access, or paid inference was performed.
- Report: `reports/2026-07-18/NEXO-0045-single-chat-orchestrator-session-001.md`.
- Closeout: `closeouts/NEXO-0045-single-chat-orchestrator.md`.

## NEXO-0046 Structured Control Engine (nexo-plan)

- Registered a read-only, standard-library control-engine slice for structured
  task manifests, operational gates, and lifecycle transition decisions.
- Kept `tasks.md` canonical; manifests intentionally duplicate status only so
  conflicts fail closed.
- Selected a direct closed transition table rather than State objects or a
  workflow framework.
- Prepared `plans/NEXO-0046-structured-control-engine.md`, its handoff, and the
  initial `state/tasks/NEXO-0046.json` manifest.
- Implemented a read-only engine for inspect, build/QA/security/close gates, and
  declared lifecycle transitions with machine-readable decisions.
- The self-hosted lifecycle blocked direct active-to-closed, authorized
  active-to-implemented, and processed two security findings through governed
  implemented-to-active rework.
- Hardened canonical evidence directories, symlink containment, exact task/plan
  identity, recorded verification commands, and unique review decisions.
- Final verification: 14/14 focused tests, 38/38 complete harness tests, compact
  context, effective OpenCode config, Graphify, QA, security, and governed close
  all pass.
- No commit, push, deploy, external mutation, dependency change, credential
  access, or paid inference was performed.
- Latest report:
  `reports/2026-07-18/NEXO-0046-structured-control-engine-session-003.md`.
- Closeout: `closeouts/NEXO-0046-structured-control-engine.md`.
- Opened `NEXO-0047` for MCP hardening after the user selected the next queued
  task.
- Verified from official sources that `chrome-devtools-mcp` stable is `1.6.0`,
  the configured GitHub npm server is deprecated, GitHub recommends its hosted
  MCP endpoint, and Context7 exposes two documentation-query tools.
- Chose exact-version local Chrome, official remote read-only/lockdown GitHub,
  and official remote keyless Context7, scoped only to the Nexo orchestrator.
- Real MCP discovery proved GitHub's remote auth incompatible with OpenCode
  dynamic client registration. Docker `29.6.1` is available, so the task moved
  to GitHub's immutable `v1.6.0` stdio image by Linux/amd64 digest with built-in
  in-memory OAuth, loopback callback, read-only/lockdown controls, and hardened
  container flags. No PAT or OAuth authorization was used.
- Added keyless Context7 with a two-tool documentation role, global MCP denies,
  orchestrator-only enables, and confirmation for Chrome control.
- Security review 001 found missing prompt-injection handling for MCP results;
  governed implemented-to-active rework added canonical untrusted-data rules
  and regression coverage.
- Final verification passed with 5/5 focused tests, 43/43 complete harness
  tests, effective config, three connected MCPs, 4,148-character context, and a
  9,126-node Graphify update.
- QA passed, security approved, and the control engine authorized close with no
  blockers. No account authentication, OAuth consent, MCP data operation,
  browser control, credential access, commit, push, deploy, or paid inference
  occurred.
- Latest report:
  `reports/2026-07-18/NEXO-0047-hardened-mcp-integrations-session-002.md`.
- Closeout: `closeouts/NEXO-0047-hardened-mcp-integrations.md`.

## NEXO-0048 Architecture And Dependency Selection Skills (nexo)

- The user explicitly selected the next Agent Workflow task instead of the
  default product focus.
- Registered `NEXO-0048` from the NEXO-0047 closeout follow-up with a governed
  manifest, plan, handoff, and separate architecture/dependency evaluations.
- Selected canonical Markdown skills plus deterministic validation in the
  existing read-only control engine; no evaluation framework or dependency is
  justified.
- The control engine authorized `planned -> active`; the task is synchronized as
  `active` before implementation begins.
- Added both canonical skills, native adapters, reusable templates, and
  fail-closed approved-decision checks. Focused tests pass and the complete
  harness now passes 50/50; effective config, skill discovery, compact context,
  the real build gate, and Graphify also pass.
- The control engine authorized `active -> implemented` with the implementation
  record, session report, and all declared verification evidence.
- Initial QA and security reviews blocked close: approved decisions were not
  scoped to one evaluation section, later transitions did not revalidate them,
  implementation-role manifest ownership was too broad, and canonical artifact
  paths lacked final in-repository directory containment.
- The control engine authorized `implemented -> active`; review 001 records are
  preserved while governed rework proceeds.
- Rework now scopes one approved decision to one exact task-bound evaluation
  section outside fenced examples, requires non-placeholder fields, revalidates
  approvals at every later gate, contains artifact realpaths, and reserves
  manifests for planner/orchestrator policy.
- Adversarial focused checks and all 61 harness tests pass. Effective config,
  skill discovery, build-agent manifest denial, compact context, the real build
  gate, and Graphify also pass; session-002 is the current implementation
  evidence.
- The hardened engine revalidated both evaluations and authorized the second
  `active -> implemented` transition with all eight verification commands.
- Review 002 found malformed fence closers could expose fenced evidence,
  QA/security close decisions still lacked exact task-bound sections, and the
  manifest deny did not yet cover every mutating specialist.
- The engine authorized another `implemented -> active` transition; QA/security
  review 002 records are preserved while final rework proceeds.
- Final rework now enforces length-aware valid Markdown fences, exact
  task-bound QA/security evaluation sections, and manifest denies across every
  mutating specialist except the planner.
- Focused checks and all 67 harness tests pass. Effective config, both runtime
  agent permission checks, skill discovery, compact context, the real build
  gate, and Graphify pass; session-003 is current implementation evidence.
- The engine revalidated both evaluations and all nine commands before
  authorizing the third `active -> implemented` transition.
- Review 003 found one remaining valid-fence opener bypass when an info string
  began with the opposite delimiter. The engine authorized
  `implemented -> active`; delimiter-specific parsing and open/closed regression
  cases were added while all prior controls remain intact.
- Delimiter-specific openers now pass 35 focused control-engine and all 70
  harness tests. Runtime config, skill discovery, specialist permissions,
  compact context, the real build gate, and Graphify pass; session-004 is the
  current implementation evidence.
- The engine revalidated both evaluations and all nine commands before
  authorizing the fourth `active -> implemented` transition.
- Final QA passed and security approved session-004 with no blocking finding.
  Exact review-004 artifacts and the closeout are ready for the governed close
  decision.
- The control engine authorized `implemented -> closed` after revalidating every
  decision, all nine verification commands, final QA/security evidence, and the
  closeout. NEXO-0048 is synchronized as closed.
- No dependency, credential, account, external mutation, commit, push, deploy,
  browser action, or paid inference was performed.
- Final report:
  `reports/2026-07-18/NEXO-0048-architecture-dependency-selection-skills-session-004.md`.
- Closeout: `closeouts/NEXO-0048-architecture-dependency-selection-skills.md`.

## NEXO-0049 OpenCode2 Productivity And Observability (nexo)

- The user explicitly approved executing all five proposed OpenCode2 phases in
  one governed task without changing the default product focus.
- Registered the active manifest, plan, handoff, architecture evaluation,
  dependency evaluation, and external approval evidence.
- Selected additive project-local adapters and native OpenCode controls; kept
  `nexo`, `harness/control/`, the control engine, and the budget ledger canonical.
- Approved only `@plannotator/opencode@0.23.1` as an external dependency, in
  manual local-only mode. No swarm, memory, auto-review, DCP, telemetry DB, or
  automatic shell-rewrite dependency is authorized.
- Build awaits the real NEXO-0049 control-engine gate.
- The real build gate passed. Exact manual Plannotator, native bounds, TUI
  attention/footer, sanitization, `.env*` guards, content-free tool telemetry,
  deterministic doctor, and optional loopback status were implemented.
- A real pseudo-TTY smoke exposed `Plugin export is not a function` for CommonJS
  entrypoints in the OpenCode2 dev loader. Thin one-function ESM adapters now
  load the directly tested CommonJS cores; the TTY then loaded server plugins
  and `tui.json` without plugin errors.
- The first full harness run found one stale FIAD test import after that move;
  it was corrected. Final focused tests and all 81 OpenCode tests pass.
- Pre-close security review found arbitrary folder/path input could bypass the
  direct-file guard through manual annotation. The command now accepts only
  project-relative `.md`, `.txt`, or `.html` files and denies URLs, folders,
  traversal, absolute paths, and `.env*`; direct regressions pass.
- Graphify updated to 9,620 nodes, 11,615 edges, and 973 communities with only
  pre-existing zero-node and optional SQL-parser warnings.
- The control engine authorized `active -> implemented`; governed QA passed and
  security approved with no unresolved high or medium finding.
- The control engine then authorized `implemented -> closed` after revalidating
  all decisions, nine commands, review evidence, and closeout. NEXO-0049 is
  synchronized as closed without changing product focus.
- No global Plannotator command, credential, OAuth grant, sharing, paid
  inference, product dependency, commit, push, deploy, or external environment
  change was made.
- Final report:
  `reports/2026-07-18/NEXO-0049-opencode2-productivity-observability-session-001.md`.
- Closeout: `closeouts/NEXO-0049-opencode2-productivity-observability.md`.
