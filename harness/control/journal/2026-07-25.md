# Journal - 2026-07-25

## NEXO-0049 OpenCode2 Stable Upgrade Follow-Up (nexo)

- Reproduced the reported `opencode2 upgrade` failure as
  `Unknown installation method: unknown`; the custom package-local install
  cannot be inferred by the built-in updater.
- Updated the runtime through `~/.opencode-dev` from
  `0.0.0-dev-202607181805` to stable `1.18.5`.
- Approved only the exact `opencode-ai@1.18.5` npm postinstall and rebuilt the
  native executable after npm 12 blocked install scripts by default.
- Both `opencode2` and `opencode` now resolve `1.18.5`; npm reports no
  unreviewed install scripts and no package vulnerabilities.
- Doctor, effective config, runtime info, startup, all 81 OpenCode harness
  tests, and a pseudo-TTY launch passed. The update dialog no longer appears.
- Documented the package-local upgrade path in the NEXO-0049 runbook. Direct
  `opencode2 upgrade` remains unsuitable for this custom installation layout.
- Report:
  `reports/2026-07-25/NEXO-0049-opencode2-upgrade-session-002.md`.

## NEXO-0031 Reversible Local Database Restore (nexo)

- Verified and restored `nexo-backup-20260726T041228Z.sql.gz` into a parallel
  local PostgreSQL database.
- Validated 78 items/photos, 9 exact Prisma migration checksums, zero orphaned
  relations, and 78/78 matching Azure blobs before cutover.
- Promoted the restored database to `nexo`; retained the previous database as
  `nexo_pre_restore_20260726` for rollback.
- Authenticated local profile, catalog, inventory, facets, statistics, and
  protected-photo checks passed.
- Documented the reversible restore convention and the pre-existing
  rate-limited API healthcheck behavior.
- Report:
  `reports/2026-07-25/NEXO-0031-local-db-restore-session-004.md`.

## NEXO-0050 Garment Lifecycle And Business Rules Plan (nexo-plan)

- Created a new P0 planned task rather than rewriting NEXO-0037; its implemented
  safe editor remains a prerequisite.
- Documented why field edits currently leave stale states and the creation bug
  that can accept `RETURNED` through the transition map.
- Defined exhaustive six-state inventory and five-state listing lifecycles,
  required garment/price/transaction facts, role guards, safe automatic
  derivation, strict different-user price approval, and append-only audit.
- Selected two persisted lifecycle dimensions inside the existing inventory
  modular-monolith boundary, with no new dependency. Added migration, command
  API, phased implementation, acceptance, QA, and security requirements.
- Registered `state/tasks/NEXO-0050.json`. Product-owner approval, a durable
  ADR, NEXO-0037 scope alignment, and a plan-to-build handoff remain required.
- Plan:
  `plans/NEXO-0050-garment-business-rules.md`.
- Report:
  `reports/2026-07-25/NEXO-0050-garment-business-rules-planning-session-001.md`.
