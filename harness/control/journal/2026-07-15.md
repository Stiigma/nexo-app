# Journal - 2026-07-15

## NEXO-0034 - Codex Project Configuration (nexo-build)

- Decisión: usar `.codex/config.toml` como capa específica del repositorio; no
  modificar `~/.codex/config.toml`.
- Trabajo completado: añadidos modelo `gpt-5.6-sol`, razonamiento `xhigh`,
  `approval_policy = "never"`, `sandbox_mode = "danger-full-access"`,
  personalidad pragmática, baja verbosidad, resumen conciso y los plugins
  `computer-use@openai-bundled` y `browser@openai-bundled`.
- Trabajo completado: preservados los MCP existentes de Chrome DevTools y GitHub.
- Compatibilidad: `notify` quedó fuera por ser global-only; la clave
  `enabled-reasoning-efforts` quedó fuera porque Codex `0.144.1` no la reconoce.
- Verificación: `codex --strict-config ... doctor --summary --no-color` cargó la
  configuración y confirmó `approval Never` y acceso irrestricto.
- No hubo commit, push, deploy, cambio externo ni persistencia de secretos.

## NEXO-0033 - Verificación de estado de importación de prendas (nexo-build)

- Decisión: conservar `NEXO-0033` como `planned`; esta sesión fue sólo de verificación solicitada por el usuario.
- Verificación: fixture listo con 39 prendas (`NX-0018` a `NX-0056`), 39 fotos y 10 costos desconocidos; la app todavía no tiene `PRICE_PENDING`, costo nullable ni script de importación.
- Verificación: PostgreSQL local permanece con 17 items y 17 fotos, sin items del lote nuevo.
- Verificación: consulta de sólo lectura en Azure Blob Storage confirma que los 36 blobs existentes fueron modificados el 2026-07-07, antes de crear el fixture el 2026-07-08; no hubo carga cloud del lote nuevo.
- No se modificaron base de datos, cloud storage, código ni secretos; tampoco se ejecutó prueba visual.
- Siguiente paso recomendado: implementar el handoff `HOFF-2026-07-08-import-price-pending-fixture.md` y detenerse en dry-run verificado antes de pedir autorización para ejecutar la importación real.

## NEXO-0033 - Inicio de implementación (nexo-build)

- Estado: `active`.
- Alcance de esta sesión: soporte de `PRICE_PENDING`, costos nulos, prioridad/filtros/estadísticas, tratamiento visual e importador idempotente dry-run-first.
- Restricción: no ejecutar `--execute`, no escribir en la base local ni en Azure sin confirmación explícita posterior.

## NEXO-0033 - Migración local autorizada (nexo-build)

- Autorización: el usuario autorizó explícitamente aplicar la migración; no autorizó todavía el importador `--execute`.
- Trabajo completado: aplicada `20260715193000_price_pending_nullable_cost` contra la base PostgreSQL local.
- Verificación: historial de Prisma al día y esquema válido; consulta de sólo lectura confirma 17 prendas, 17 fotos, 0 prendas `PRICE_PENDING` y 0 costos nulos antes de importar.
- Verificación: el dry-run vuelve a informar 39 prendas, 39 fotos WebP, 39 precios de venta nulos y 10 costos nulos, sin escribir en BD ni Azure/local storage.
- Siguiente paso: esperar autorización explícita y separada para ejecutar `pnpm db:import:inventory-fixture -- --execute`; después hacer QA visual autenticado.

## NEXO-0033 - Importación autorizada y QA parcial (nexo-build / nexo-qa / nexo-security)

- Autorización: el usuario autorizó ejecutar el siguiente paso del flujo, incluido `--execute`.
- Trabajo completado: importadas 39 prendas (`NX-0018` a `NX-0056`) y vinculadas 39 fotos principales WebP en Azure Blob Storage; la ejecución reportó 39 creadas y 0 actualizadas.
- Integridad: la base local contiene 56 prendas; el lote contiene 39 `PRICE_PENDING`, 39 precios objetivo y mínimos nulos, 10 costos nulos, cero costos en `0`, y 39 rutas `items/{itemId}/main.webp`.
- QA: el navegador local llegó a la pantalla de inicio de sesión; no se transmitieron credenciales. Falta la comprobación visual autenticada de las tarjetas, filtro, foto y detalle.
- Seguridad: revisión condicional registrada. Las rutas guardan SAS de sólo lectura por HTTPS, con expiración configurada de siete días; se requiere aceptación explícita de ese riesgo existente o una solución de refresco de URLs antes del cierre.

## NEXO-0035 - Plan de eficiencia de tokens con Terra (nexo-plan)

- Hallazgo: el arranque normal de Nexo solicita aproximadamente 48,700
  caracteres, equivalentes a unos 12,200 tokens estimados, antes de leer código.
- Hallazgo: `isyte-ops` inyecta contexto FIAD globalmente; el budget guard
  atribuye costo a la última tarea activa y no existe todavía un ledger real.
- Hallazgo: el ejecutable local de OpenCode no inicia porque falta su paso de
  postinstall; no se reparó ni reinstaló.
- Decisión: usar Terra con contexto compilado determinísticamente, verbosidad
  baja y razonamiento por riesgo (`medium` operativo, `high` normal, `xhigh`
  sólo en decisiones críticas), manteniendo pruebas, QA y security gates.
- Trabajo: creados plan, handoff y reporte para `NEXO-0035`; no se cambió
  configuración, plugins, producto, entorno ni proveedor.
- Siguiente paso: esperar aprobación para implementar primero el compilador de
  contexto y el aislamiento FIAD sin realizar llamadas pagadas.

## NEXO-0036 - Gateway autorizado para fotos renovables (nexo-plan)

- Decisión: remediar el hallazgo SEC-1 de NEXO-0033; no aceptar URLs SAS
  persistidas que vencen.
- Patrón aceptado: guardar únicamente `storageKey` y emitir un redirect de
  lectura corto, autenticado y `no-store` por solicitud de foto. No habrá un
  cron que renueve tokens almacenados.
- Artefactos creados: plan, handoff y ADR para separar la identidad estable del
  blob de la capacidad temporal de lectura, con el proveedor detrás de la
  costura `FileStoragePort`.
- Siguiente paso: nexo-build ejecuta el handoff; la aplicación de la migración
  de datos sigue requiriendo confirmación explícita del usuario.

## NEXO-0036 - Implementación y revisión condicional (nexo-build / nexo-qa / nexo-security)

- Implementación: `ItemPhoto` pasó a representar `storageKey`; el gateway
  `GET /media/photos/:photoId/content` autentica/autoriza y redirige con
  `private, no-store` a una URL de lectura Azure corta, HTTPS y read-only.
- Desacoplamiento: los inventarios sólo reciben metadata de foto; el frontend
  compone la ruta estable por ID y ni el dominio de inventario ni el navegador
  construyen Azure/SAS. `ResolveItemPhotoReadUrlUseCase` concentra el lookup,
  proveedor y política de TTL detrás de `FileStoragePort`.
- Verificación: Prisma válido; 14 archivos/61 pruebas backend, build backend y
  frontend, typecheck estricto de ambos importadores y dry-run pasaron.
- Datos: consulta de sólo lectura identificó 56 URLs Azure firmadas; simulación
  de normalización convierte 56/56 a `items/{id}/main.webp`, sin residuos de
  URL/query/ruta pública.
- Seguridad/QA: aprobaciones condicionales registradas. Permanece verificar
  privacidad del container Azure y UI autenticada después de aplicar migración.
- No se aplicó la migración, no hubo DB/Azure writes, commit, push, deploy ni
  cambio de configuración externa. Siguiente: pedir autorización explícita
  para `20260715210000_item_photo_storage_key`.

## NEXO-0037 - Preparación de inventario para venta (nexo-plan)

- Decisión propuesta: mantener dos ciclos independientes: inventario físico
  (`PRICE_PENDING`/`ACQUIRED_STOCK` a `AVAILABLE`, `RESERVED`, `SOLD`) y ciclo
  comercial (`DRAFT`, `READY_FOR_REVIEW`, `PUBLISHED`, `PAUSED`).
- Trabajo: creado el plan de ficha comercial, controles de precio, cola de
  edición de las 39 prendas `PRICE_PENDING` y responsabilidades operativas.
- Permisos: se conserva `ADMIN`/`OPERATOR` en v1. Editor y vendedor se mapean a
  `OPERATOR`; aprobación de precios, publicación, costos y correcciones queda
  en `ADMIN`.
- No se cambió código, base de datos, Azure, configuración, credenciales ni
  servicios externos.
- Siguiente: confirmar la política comercial propuesta y enviarla a
  `nexo-spec` para resolver `OQ-007` y actualizar requisitos y trazabilidad.

## NEXO-0036 - Migración y verificación técnica autorizadas (nexo-build / nexo-qa / nexo-security)

- Autorización: el usuario autorizó implementar el plan, incluida la migración
  local pendiente `20260715210000_item_photo_storage_key`.
- Datos: migración aplicada; las 56 fotos almacenan claves canónicas
  `items/{id}/main.webp`, sin URLs Azure, consultas SAS ni rutas públicas
  residuales.
- Seguridad: se generó una URL de lectura nueva únicamente bajo demanda,
  HTTPS y de sólo lectura; una solicitud sin firma no obtuvo contenido público.
  No se cambiaron contenedor, blobs, credenciales, Azure, despliegue ni DNS.
- Corrección: se hizo explícita la inyección de `PrismaService` del resolver de
  medios para que Nest arranque el gateway en tiempo de ejecución. La ruta
  protegida responde `401` sin sesión.
- Verificación: Prisma válido y al día; 61 pruebas backend, compilación backend
  y compilación frontend correctas. Persiste sólo la advertencia existente de
  tamaño de bundle en frontend.
- Pendiente: QA visual con sesión de operador para tarjetas, detalle y lightbox
  en `/admin/inventory`; no se deben introducir ni suplantar credenciales.

## NEXO-0037 - Editor seguro de prendas (nexo-spec / nexo-design / nexo-build / nexo-security)

- Autorización y política: la persona usuaria autorizó implementar primero el
  editor y confirmó que `ADMIN` puede hacer las mismas acciones que `OPERATOR`.
  Costos, precios mínimos, códigos, compras y estados físicos permanecen fuera
  del editor.
- Requisitos: se resolvió `OQ-007`; quedaron registrados US-023 (editor) y
  US-024 (ciclo comercial posterior), más diseño, trazabilidad y handoff.
- Implementación: `PUT /inventory/items/:id/editor` permite editar datos
  seguros; `/inventory` ya puede usarlo con ambas funciones. Se muestra una
  lista de faltantes de ficha, sin publicar ni vender.
- Seguridad: al detectar que la pantalla compartida podía revelar costos, se
  aplicó redacción en API para `OPERATOR` y ocultamiento de interfaz. SEC-0037-1
  quedó remediado; la revisión es condicional hasta QA autenticado.
- Verificación: 64 pruebas backend, builds de backend/frontend y validación de
  Prisma pasaron. No se cambió base de datos, Azure, configuración externa,
  credenciales, commit, push ni despliegue.
- Siguiente: validar visualmente con una sesión de operador y otra de admin;
  después construir revisión/publicación de la ficha comercial.

## NEXO-0035 - Implementación del flujo eficiente con Terra (nexo-build / nexo-qa / nexo-security)

- Autorización: el usuario aprobó ejecutar la implementación local; reparar
  OpenCode y hacer llamadas pagadas permanecieron fuera de alcance.
- Implementación: foco canónico, compilador determinístico de contexto con
  límite de 10,000 caracteres, fallback seguro, arranque compacto, Terra con
  verbosidad baja y razonamiento por riesgo.
- Aislamiento: las sesiones Nexo ya no reciben contexto FIAD; los marcadores
  FIAD sobreviven compactación/recarga y un comando Nexo los elimina.
- Presupuesto: cada sesión se liga a una tarea explícita; sesiones sin vínculo
  usan `UNBOUND_SESSION`; el ledger agrega costo/tokens por modelo, agente y
  fase sin perder los límites soft/hard existentes.
- Verificación: 18 pruebas sintéticas pasan; la metadata local confirma Terra
  y variantes `medium`, `high`, `xhigh`. QA condicional y security aprobado.
- Sin llamadas pagadas, reparación/reinstalación, secretos, producto, BD,
  Azure, commit, push ni deploy.
- Siguiente: usar el paquete compacto con foco NEXO-0036; validar resolución
  real de OpenCode sólo después de autorización separada para reparar el CLI.

## NEXO-0036 - Incidencia de respuestas 500 reportadas (nexo-qa)

- Hallazgo reportado: el navegador recibió `500` para varios catálogos y rutas
  protegidas de contenido de foto desde el runner en el puerto 5173.
- Reproducción intentada: tres solicitudes directas al mismo endpoint de
  catálogos sin sesión respondieron `401` de forma consistente; esto confirma
  la barrera de autenticación, pero no reproduce el `500` del usuario.
- Decisión: no inferir ni aplicar una corrección sin una reproducción
  autenticada y el error de servidor correspondiente.
- Pendiente: iniciar sesión localmente y repetir la carga para capturar y
  corregir la causa exacta; no hubo cambios de producto, base de datos, Azure,
  credenciales, commit, push ni despliegue.
