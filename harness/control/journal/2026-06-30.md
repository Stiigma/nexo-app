# Journal - 2026-06-30

## NEXO-0003 - Requirements Spec Section

- Decision: create `docs/spec/` as the implementable requirements layer while
  keeping `NEXO_PROJECT.md` as the product source document.
- Decision: use requirement IDs, priority, MoSCoW, story points, acceptance
  criteria, and traceability.
- Decision: mark unresolved product policies as open questions instead of
  inventing hidden behavior.
- Work completed: created SRS, user stories, traceability matrix, and templates.
- Work completed: updated control-plane live state and task index.
- Result: `NEXO-0003` closed.
- Next recommended task: review open questions in `docs/spec/SRS.md`, then
  continue `NEXO-0002` for `CONTEXT.md`.

## NEXO-0004 - Agentic Harness Upgrade

- Decision: register `NEXO-0004` to upgrade `harness/control/` from a control
  ledger into the canonical agent operating system for Nexo.
- Decision: keep `harness/control/` canonical and add `.opencode/` as an
  adapter layer rather than a competing source of truth.
- Decision: keep Codex support repo-contained through `AGENTS.md` and
  control-plane documentation.
- Work started: create canonical agents, skills, state, indexes, templates,
  OpenCode adapters, and verification records.
- Work completed: created canonical agent docs for planning, build,
  requirements, design, QA, infrastructure, and security.
- Work completed: created canonical skills for memory/resume, task start,
  logging, handoff, closeout, requirements trace, QA review, security review,
  design spec, and infra guardrails.
- Work completed: added state, indexes, handoff, implementation,
  investigation, runbook, security, template, and OpenCode adapter scaffolding.
- Work completed: updated `AGENTS.md`, `README.md`, `WORKFLOW.md`, and
  `tasks.md` with shared routing and guardrails.
- Verification: expected files exist; `opencode.json` is valid JSON; routing,
  continuity, handoff, QA, and security gate docs are present; ASCII and
  secret-pattern scans found no matches.
- Verification note: `git status --short` could not run because the workspace
  is not recognized as a Git repository.
- Result: `NEXO-0004` closed.
- Next recommended task: review open questions in `docs/spec/SRS.md`, then
  continue `NEXO-0002` for `CONTEXT.md`.

## NEXO-0002 - Domain Context Document

- Decision: activate `NEXO-0002` and run it with `grill-with-docs`, using
  `domain-modeling` for the glossary.
- Decision: use English technical domain terms as canonical language for future
  code, tests, and handoffs, while documenting Spanish synonyms to avoid when
  they conflict.
- Decision: keep `CONTEXT.md` as domain language, not an implementation spec.
- Work started: created the first `CONTEXT.md` draft from confirmed
  `NEXO_PROJECT.md` and `docs/spec/` language.
- Next step: resolve open domain decisions one at a time, starting with how to
  model final sale price when a sale contains multiple garments.
- Decision: add `Sale Line` as the canonical term for assigning one final sale
  price to one sold garment inside a sale, so profit per garment is
  deterministic.
- Work completed: created
  `reports/2026-06-30/NEXO-0002-domain-context-grill-session-001.md` with the
  decisions from the first inventory-focused grill session.
- Decision: prioritize inventory capture for the upcoming USA buying trip over
  detailed sales/profit workflows.
- Decision: separate inventory state from listing status.
- Decision: use a manual purchase cart before payment; everything remaining in
  the cart at confirmation becomes acquired stock.
- Decision: purchase batch means one store/payment.
- Decision: require purchase evidence and a required difference reason when the
  real ticket total does not match the expected cart total.
- Next step: apply these decisions to `CONTEXT.md`, then continue the grill on
  quick category behavior.
- Work completed: updated `CONTEXT.md` with inventory-first domain language for
  purchase cart capture, purchase batches, acquired stock, listing statuses,
  purchase evidence, difference reasons, capture IDs, and internal codes.
- Work completed: created
  `reports/2026-06-30/NEXO-0002-domain-context-session-002.md`.
- Next step: continue the grill on whether quick category in the purchase cart
  should use the formal category catalog or a provisional capture category.
- Decision: use the formal `Category` operational catalog for garment
  classification, but do not make category blocking in the `Purchase Cart`.
- Decision: use `Category Review` for garments captured quickly without a
  reliable formal category; a formal category is required before the garment
  can satisfy the minimum garment file.
- Decision: do not create free-text or provisional category catalog values from
  purchase capture.
- Work completed: updated `CONTEXT.md` with catalog language for catalog
  values, category review, and expanded operational catalog terms.
- Work completed: created
  `reports/2026-06-30/NEXO-0002-domain-context-session-003.md`.
- Next step: continue remaining domain-language questions: listing lifecycle,
  rounding, exchange-rate fallback, QR payload, and admin corrections.
- Work completed: reconciled `docs/spec/SRS.md`, `docs/spec/user-stories.md`,
  and `docs/spec/traceability.md` with the current `CONTEXT.md`.
- Decision: requirements now use `Purchase Cart` before payment, confirmed
  `Purchase Batch` after payment, `Acquired Stock` before availability, and
  `Sale Line` for garment-level final sale price.
- Decision: removed `OQ-002` from open questions because `Sale Line` resolves
  garment-level sale price splitting.
- Work completed: created
  `reports/2026-06-30/NEXO-0002-requirements-reconciliation-session-004.md`.
- Next step: resolve remaining SRS open questions: rounding, exchange-rate
  fallback, admin corrections, duplicate customers, QR payload, and listing
  lifecycle.
