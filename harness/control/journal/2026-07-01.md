# Journal - 2026-07-01

## NEXO-0002 - Demo Orientation

- Decision: keep `NEXO-0002` active; do not start product implementation
  before a narrow plan-to-build handoff exists.
- Decision: use the purchase-cart to acquired-stock flow as the safest first
  demo slice because it follows the latest inventory-first domain decisions.
- Work completed: reviewed the control plane, active task, active plan,
  previous journal, current state, SRS open questions, user stories, and
  traceability.
- Work completed: created
  `harness/control/handoffs/HOFF-2026-07-01-purchase-capture-demo.md` as the
  draft plan-to-build handoff for a meeting demo.
- Decision: use React, SQLite WASM, and Zustand for the disposable prototype,
  keeping it separate from the target NestJS/PostgreSQL product architecture.
- Work completed: created
  `docs/adr/ADR-2026-07-01-disposable-prototype-stack.md` and updated the demo
  handoff with the confirmed stack.
- Work completed: saved the user-provided logo at `docs/brand/nexo-logo.png`
  and created `docs/brand/README.md` with prototype design notes.
- Decision: use `/home/otomi/Downloads/Backup/Harness/diseno-harness` as a
  UX/UI process and review reference for the disposable demo, not as a UI
  component package.
- Work completed: created `docs/design/README.md` and
  `docs/design/purchase-capture-demo-brief.md` to adapt the external design
  harness to the Nexo prototype.
- Files changed: `harness/control/README.md`, `harness/control/tasks.md`,
  `harness/control/state/CURRENT.md`, `harness/control/state/NEXT.md`,
  `harness/control/indexes/records.md`,
  `harness/control/handoffs/HOFF-2026-07-01-purchase-capture-demo.md`,
  `harness/control/plans/NEXO-0002-domain-context.md`,
  `docs/adr/ADR-2026-07-01-disposable-prototype-stack.md`,
  `docs/brand/README.md`, `docs/brand/nexo-logo.png`,
  `harness/control/journal/2026-07-01.md`,
  `harness/control/reports/2026-07-01/NEXO-0002-demo-orientation-session-005.md`,
  `harness/control/reports/2026-07-01/NEXO-0002-prototype-stack-session-006.md`,
  `harness/control/reports/2026-07-01/NEXO-0002-brand-reference-session-007.md`,
  `docs/design/README.md`,
  `docs/design/purchase-capture-demo-brief.md`, and
  `harness/control/reports/2026-07-01/NEXO-0002-design-harness-session-008.md`.
- Verification: confirmed product implementation has not started and remaining
  open questions are rounding, exchange-rate fallback, admin corrections,
  duplicate customers, QR payload, and listing lifecycle.
- Open items: scaffold the disposable prototype; resolve or defer the remaining
  open questions that affect the first slice.
- Next recommended step: build `prototypes/purchase-capture-demo/` with React,
  SQLite WASM, and Zustand.

## NEXO-0002 - Purchase Cart Shell Implementation

- Work completed: implemented Feature 1 under
  `prototypes/purchase-capture-demo/` as a disposable Vite + React +
  TypeScript app.
- Work completed: added Tailwind through `@tailwindcss/vite`, Zustand workflow
  state, SQLite WASM browser persistence, a repository/data-access boundary,
  validation, and a local simulated USD/MXN exchange-rate provider.
- Work completed: implemented Spanish operational screens for cart list, empty
  state, create, detail, edit, demo seed, demo reset, and a disabled future
  garment capture section.
- Work completed: copied `docs/brand/nexo-logo.png` into the prototype public
  assets and used it in a compact mobile header.
- Work completed: added Vitest tests for repository create/list/get/update,
  seed/reset, validation, and the non-network exchange-rate provider.
- Verification: `npm run test` passed with 3 test files and 11 tests.
- Verification: `npm run build` passed.
- Verification gap: manual browser/mobile review did not run because local
  Vite binding failed with sandbox `EPERM` and the escalated server request was
  interrupted by the user.
- Files changed: `prototypes/purchase-capture-demo/`,
  `harness/control/implementations/NEXO-0002-purchase-cart-shell.md`,
  `harness/control/reports/2026-07-01/NEXO-0002-purchase-cart-shell-session-009.md`,
  `harness/control/README.md`, `harness/control/tasks.md`,
  `harness/control/state/CURRENT.md`, `harness/control/state/NEXT.md`,
  `harness/control/indexes/records.md`,
  `harness/control/plans/NEXO-0002-domain-context.md`, and this journal.
- Decision: Feature 2 should be planned before implementation and should add
  `Purchase Cart Item` capture while keeping payment confirmation, purchase
  batches, acquired stock, QR, sales, reservations, auth, deployment, object
  storage, and real exchange-rate integration deferred.
- Next recommended step: start a planning session for Feature 2 with
  `nexo-plan` and `nexo-design`.

## NEXO-0002 - Purchase Cart Item Capture

- Work completed: created
  `harness/control/handoffs/HOFF-2026-07-01-purchase-cart-item-capture.md`
  as the scoped Feature 2 build handoff.
- Work completed: implemented Feature 2 under
  `prototypes/purchase-capture-demo/` with SQLite-backed `Purchase Cart Items`,
  seeded formal categories, generated per-cart `Capture ID` values, photo
  placeholders, purchase costs, optional category selection, derived `Category
  Review`, item add/edit/remove screens, and provisional totals.
- Work completed: added repository, validation, and calculation tests for item
  table initialization, category seed, item CRUD, capture ID sequencing, reset,
  demo seed items, required photo placeholder, positive purchase cost, invalid
  categories, USD/MXN totals, and no stored rounding policy.
- Decision: use `purchase_carts.next_capture_sequence` in the disposable
  SQLite schema so deleted capture IDs are not reused by normal item creation.
- Decision: keep item totals display-only while `OQ-001` remains unresolved;
  no durable rounding policy was added.
- Verification: `npm run test` passed with 4 test files and 20 tests.
- Verification: `npm run build` passed.
- Verification: sandboxed Vite startup failed with `EPERM` on
  `127.0.0.1:5173`; escalated Vite startup succeeded at
  `http://127.0.0.1:5174/`, and escalated `curl -I` returned HTTP 200.
- Verification gap: full visual mobile review remains incomplete because no
  local Chromium/Chrome binary or Playwright package is available in the
  environment.
- Files changed: `prototypes/purchase-capture-demo/`,
  `harness/control/handoffs/HOFF-2026-07-01-purchase-cart-item-capture.md`,
  `harness/control/implementations/NEXO-0002-purchase-cart-item-capture.md`,
  `harness/control/reports/2026-07-01/NEXO-0002-purchase-cart-item-capture-session-010.md`,
  `harness/control/plans/NEXO-0002-domain-context.md`,
  `harness/control/README.md`, `harness/control/tasks.md`,
  `harness/control/state/CURRENT.md`, `harness/control/state/NEXT.md`,
  `harness/control/indexes/records.md`, and this journal.
- Next recommended step: complete real mobile/browser review of
  `http://127.0.0.1:5174/`, then choose between Feature 3 payment confirmation
  planning and returning to the remaining SRS open questions.

## NEXO-0002 - Payment Confirmation & Purchase Batch

- Decision: proceed with option A (fix delete + Feature 3) from the Feature 2
  review.
- Work completed: fixed missing delete confirmation in cart item removal with
  `window.confirm()` dialog.
- Work completed: created scoped Feature 3 build handoff
  `harness/control/handoffs/HOFF-2026-07-01-payment-confirmation-batch.md`.
- Work completed: implemented Feature 3 with payment confirmation screen,
  purchase batch creation, internal code assignment, and acquired-stock
  inventory views.
- Work completed: extended domain types (Garment, PurchaseBatch,
  DifferenceReason, PurchaseEvidence, InventoryState), validation (payment
  confirmation rules), schema (v3 with purchase_batches, garments,
  difference_reasons), repository (confirmCartAsBatch, list methods), store
  (new screens and actions), and UI (PaymentConfirmForm, BatchSummary,
  AcquiredStockList).
- Decision: use deterministic store prefixes (GW, BUR, ROS, SAL) for internal
  code generation.
- Decision: use cataloged difference reasons with "Otro" requiring a note.
- Decision: keep batch creation irreversible in the demo.
- Verification: `npm run test` passed with 4 test files and 27 tests.
- Verification: `npm run build` passed.
- Verification: Vite server at `http://127.0.0.1:5174/` returns HTTP 200.
- Verification gap: full visual mobile review remains incomplete; no
  integration test for confirmCartAsBatch.
- Files changed: `prototypes/purchase-capture-demo/` (types, validation,
  repository, store, App, CartDetail, plus new PaymentConfirmForm,
  BatchSummary, AcquiredStockList), `harness/control/` (handoff,
  implementation, report, plan, state, tasks, README, journal).
- Next recommended step: complete real mobile/browser review, then decide
  between resolving SRS open questions or starting product architecture.

## NEXO-0002 — Batch Multi-Payment Domain Grill

- Decision: run `grill-with-docs` to resolve the post-payment domain model.
- Decision: Purchase Batch = 1 store, 1 day, N Payments (not 1:1 cart-to-batch).
- Decision: Purchase Cart is ephemeral; disappears after confirmation.
- Decision: Home screen shows batches, not carts.
- Decision: Multi-payment: operator can add a new cart payment to an existing
  same-store same-day batch or create a new batch.
- Decision: Each payment keeps its own tax, FX, evidence, and difference reason.
  Batch shows consolidated totals.
- Decision: Canonical term for sub-entity is `Payment`.
- Decision: Internal codes remain global per store (GW-001...), independent of
  batch or payment.
- Work completed: created ADR
  `docs/adr/ADR-2026-07-01-purchase-batch-multi-payment.md`.
- Work completed: updated `CONTEXT.md` with Payment term, redefined Purchase
  Batch, marked Purchase Cart ephemeral.
- Work completed: created full-rewrite handoff
  `harness/control/handoffs/HOFF-2026-07-01-purchase-batch-multi-payment.md`
  and detailed implementation steps
  `harness/control/handoffs/HOFF-2026-07-01-batch-multi-payment-steps.md`.
- Work completed: updated `harness/control/state/CURRENT.md`, `NEXT.md`,
  journal, and created grill report
  `harness/control/reports/2026-07-01/NEXO-0002-batch-multi-payment-grill-session-013.md`.
- Next recommended step: start a clean `nexo-build` session and execute the
  batch+payment rewrite using the handoff.

## NEXO-0002 — Batch+Multi-Payment Rewrite (Session 014)

- Decision: execute the full prototype rewrite from the Batch+Multi-Payment
  handoffs.
- Work completed: rewrote `prototypes/purchase-capture-demo/` to v4
  Batch+Multi-Payment model (1 batch = N payments, ephemeral carts,
  batch-centric home screen).
- Work completed: implemented schema v4 with `payments` table between
  `purchase_batches` and `garments`; `garments` now reference `payment_id`.
- Work completed: `confirmCartAsBatch` with explicit `batchMode` (new,
  existing, auto find/create); cart deleted after confirmation in same
  transaction.
- Work completed: new components `BatchList` (home), `BatchDetail`
  (payments+garments), `NewCartFlow` (store selector), `CartCapture`
  (item capture with discard); adapted `PaymentConfirmForm` with batch
  selector, `AcquiredStockList` for v4 types.
- Work completed: removed obsolete components `CartList`, `CartDetail`,
  `CartForm`, `BatchSummary`.
- Work completed: 41 tests across 4 files (including new tests for
  multi-payment batch confirmation, batchMode, consolidated totals,
  seedDemoBatches, cart deletion).
- Verification: `npm run test` 41/41 passed; `npm run build` clean.
- Verification: Vite dev server at `http://127.0.0.1:5174/` returns HTTP 200.
- Verification gap: full visual mobile/browser review remains incomplete.
- Files changed: domain types, validation, cartTotals, repository, store,
  App.tsx, 4 new components, 3 adapted components, 4 removed components,
  3 test files, implementation record, report, state/CURRENT.md,
  state/NEXT.md, README.md, tasks.md, and this journal.
- Next recommended step: complete mobile/browser review, then decide between
  resolving SRS open questions or starting product architecture.

## NEXO-0002 — UI/UX Redesign Proposal (Session 015)

- Decision: el prototipo actual se percibe "AI-genérico"; se produce una
  propuesta de rediseño visual y de información antes de seguir añadiendo
  features.
- Decision: adoptar `shadcn/ui` (Tailwind + Radix) como librería de componentes
  propios/editables/accesibles, reemplazando el `ui.tsx` hecho a mano.
- Decision: dirección visual chrome oscuro + canvas claro (superficies sólidas,
  no glassmorphism) para dejar de verse genérico y mejorar lectura de
  fotos/reportes; modo oscuro queda como fase 2.
- Decision: introducir sidebar + topbar + canvas responsive (Sheet en móvil,
  rail en tablet, expandido en escritorio) para dar hogar a inventario, ventas,
  gastos y reportes.
- Decision: objetividad de pantalla (una pantalla = un propósito = una acción
  primaria) con separación de entidades (Tabs/Data-Table en S6/S7) y
  revelación progresiva (totales colapsables, acciones secundarias en menús).
- Decision: política de copys en español con términos del dominio como
  sustantivos propios traducidos (propuesta §12), eliminando la mezcla
  inglés/español actual.
- Work completed: auditó la implementación actual (App.tsx, ui.tsx, styles.css,
  7 componentes) y documentó 10 causas del aspecto genérico (D1–D10).
- Work completed: creó `docs/design/nexo-ui-redesign-proposal.md` con
  diagnóstico, principios, librería, tokens de color/tipografía/radios,
  tratamiento de logo, layout responsive, rediseño S1–S7, inventario de
  componentes, estados, accesibilidad, copys y plan de implementación 3 fases.
- Work completed: creó handoff plan-to-build
  `harness/control/handoffs/HOFF-2026-07-01-ui-redesign.md` para `nexo-build`.
- Note: este modelo no soporta lectura de imágenes; el tratamiento del logo se
  basó en `docs/brand/README.md` (fondo negro, wordmark italic blanco, barra
  azul eléctrico).
- Files changed: `docs/design/nexo-ui-redesign-proposal.md` (NEW),
  `harness/control/handoffs/HOFF-2026-07-01-ui-redesign.md` (NEW),
  `harness/control/journal/2026-07-01.md` (APPEND),
  `harness/control/reports/2026-07-01/NEXO-0002-ui-redesign-proposal-session-015.md` (NEW).
- Open items: confirmar default modo claro vs oscuro; generar `nexo-mark.svg`
  en Fase 0; revisión visual real pendiente (sin Chromium/Playwright).
- Next recommended step: sesión `nexo-build` que ejecute el handoff (Fase 0:
  shadcn + tokens), con verificación `nexo-qa` al cierre.

## NEXO-0002 — UI/UX Redesign Build (Session 016)

- Decision: ejecutar el handoff `HOFF-2026-07-01-ui-redesign.md` y la propuesta
  `docs/design/nexo-ui-redesign-proposal.md` con `nexo-build`.
- Decision: adoptar `shadcn/ui` (new-york) copiando los componentes al repo y
  temátizandolos para Nexo (no CLI), siguiendo la filosofía "copy & own".
- Decision: `NativeSelect` nativo estilado en lugar de Radix `Select` para
  preservar `value=""` (categoría/lote existente) y no tocar lógica de
  state/domain.
- Decision: canvas claro por defecto; modo oscuro con tokens definidos pero sin
  toggle (fase 2).
- Work completed (Fase 0): instalé Radix/cva/clsx/tailwind-merge/sonner/cmdk/
  tw-animate-css; `src/lib/utils.ts` (`cn`); token system en `styles.css`
  (chrome oscuro + canvas claro, estados, sidebar=chrome, reduced-motion);
  alias `@/`; `components.json`; `public/nexo-mark.svg`; 22 primitivos
  `shadcn/ui` en `src/components/ui/*`.
- Work completed (Fase 1): compuestos Nexo (`NexoAppShell`, `NexoSidebar`,
  `Topbar`, `StatusBadge`, `Money`, `Field`, `PhotoThumb`, `EntityCard`,
  `ContextStrip`, `StickyActionBar`, `EmptyState`, `StepHeader`,
  `DifferenceAlert`, `NavItem`/`NavGroup`); shell responsive (rail/expandido/
  Sheet) + topbar + Toaster; `App.tsx` delega al shell; `notice`→toast,
  `error`→Alert, `LoadingSkeleton`.
- Work completed (Fase 2): migré S1–S7 a superficies sólidas, regiones,
  revelación progresiva, `Money`, `StatusBadge`, copys en español y
  `AlertDialog` (`ConfirmDialog`); eliminé `src/components/ui.tsx`.
- Work completed (Fase 3): `EmptyState` con guía, `Skeleton`, `Spinner`,
  `Forbidden`, breadcrumb con `<button>` navegables, meta
  `viewport-fit=cover` + `theme-color`, auditoría de aria-labels.
- Verification: `npm run test` 43/43 pasa; `npm run build` limpio; Vite dev
  server HTTP 200 en `http://127.0.0.1:5175/`; assets de marca sirven; sin
  errores de transformación.
- Verification gap: revisión visual/móvil real pendiente (sin
  Chromium/Playwright en el entorno).
- Files changed: `prototypes/purchase-capture-demo/` (config, styles, App,
  22 ui/* NEW, 17 nexo/* NEW, 7 pantallas migradas, ui.tsx DEL, hooks NEW,
  mark.svg NEW), `harness/control/` (implementation, report, state, tasks,
  README, indexes, journal).
- Open items: QA visual/accesibilidad por `nexo-qa`; confirmar commit con el
  usuario; command palette placeholder (cmdk instalado); S7 costo en USD
  hardcodeado (limitación preexistente).
- Next recommended step: QA visual/accesibilidad del prototipo rediseñado,
  luego decidir entre open questions del SRS o arquitectura del producto
  reutilizando los tokens/componentes Nexo como base de `front/`.
