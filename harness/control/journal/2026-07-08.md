# Journal - 2026-07-08

## NEXO-0030 - Command Code Fallback For OpenCode (nexo-infra)

- Decision: configure Command Code (`cmd`) as a practical fallback for OpenCode
  using Nexo's canonical control plane rather than trying to replicate OpenCode
  plugin internals.
- Work completed: registered `NEXO-0030`, created plan, runbook, implementation
  record and session report.
- Work completed: reinforced `.commandcode/taste/taste.md` so Command Code
  starts from `AGENTS.md`, `harness/control/README.md`, `WORKFLOW.md`,
  `tasks.md`, active plan/handoff, today's journal and state files before
  working.
- Work completed: configured project MCPs through `cmd mcp add`:
  `chrome-devtools` and `github`, persisted in `.mcp.json`.
- Verification: `cmd --version` returned `0.41.2`; `cmd status` authenticated;
  `cmd --list-models` included `deepseek/deepseek-v4-pro`; `cmd mcp list`
  showed both MCPs enabled with project scope.
- Verification: non-mutating `cmd -p --max-turns 2` smoke test passed after
  approved escalation for normal `~/.commandcode` session writes. Output named
  active tasks `NEXO-0008` and `NEXO-0023` and restated workflow restrictions.
- Guardrail: GitHub MCP token, if needed, stays in the operator environment and
  must not be written to repo files.
- Next: use the runbook when OpenCode is unavailable; avoid mutating build
  prompts unless a Nexo task and handoff are clear.

## NEXO-0031 - CI/CD, Deploy & Domain (nexo-plan → nexo-infra)

- Context: handoff `HOFF-2026-07-07-cicd-deploy-domain.md` created; NEXO-0031
  registrado en `tasks.md` como `active`.
- Config change: `.opencode/agents/nexo-plan.md` y `.opencode/agents/nexo-infra.md`
  editados para permitir delegación de `nexo-infra` desde `nexo-plan`
  (permission blocks + `mode: all` + tool allow-list). El `Task` tool ya acepta
  el subagent.
- Execution: `nexo-plan` delegó la creación de archivos Fase 1 a `nexo-infra`.
  Creados: `back/Dockerfile`, `back/.dockerignore`, `back/.github/workflows/nexo-api-ci.yml`,
  `infra/docker-compose.yml` (servicios `nexo-api`+`nexo-ngrok`), `infra/.env.example`,
  `front/vercel.json`, `front/.github/workflows/nexo-app-cicd.yml`,
  `front/.env.production.example`, `.github/workflows/nexo-db-backup.yml`,
  runbook operacional y 3 ADRs. `docker compose config` exit 0. Cero secretos reales.
- Pending: push GitHub, deploy Vercel, ngrok token real, DNS `teamnexo.nexoshopmx.store`,
  credenciales Neon/Azure, y QA + Security review antes de close.
- Report: `reports/2026-07-08/NEXO-0031-infra-handoff-execution.md`.

## NEXO-0032 - Price-Pending Inventory Fixture (nexo-build)

- Work completed: created `../fixtures/inventory/manual-stock-2026-07-08-price-pending/` from current `newstorage/item*` folders.
- Work completed: expanded source ranges into 39 physical items, `NX-0018` through `NX-0056`, with 39 canonical photos and source audit copies under `_source/`.
- Decision: every item uses fixture-only `status: price_pending`; fixture must not be imported until real app `PRICE_PENDING` support exists.
- Decision: known source costs are USD purchase costs; missing costs remain `null` with `review.needs_cost: true`; `item31-32` cost split as 6 USD per piece.
- Verification: manifest/filesystem validation passed for counts and paths; price fields are null; no zero cost placeholders; contact sheet reviewed.
- Report: `reports/2026-07-08/NEXO-0032-price-pending-inventory-fixture-session-001.md`.
- Closeout: `closeouts/NEXO-0032-price-pending-inventory-fixture.md`.

## NEXO-0033 - Import Price-Pending Fixture Planning (nexo-plan)

- Decision: register the next fixture import as planned task `NEXO-0033`, not active implementation.
- Work completed: saved implementation-ready plan `plans/NEXO-0033-import-price-pending-fixture.md`.
- Work completed: saved first-run handoff `handoffs/HOFF-2026-07-08-import-price-pending-fixture.md` for `nexo-build`.
- Decisions captured: real `PRICE_PENDING`, nullable unknown costs, `Sin marca`, precise category upserts, and WebP optimization before storage upload.
- Guardrail: real DB/storage import requires explicit user confirmation and explicit `--execute`; next agent should start with dry-run.
- Report: `reports/2026-07-08/NEXO-0033-import-price-pending-fixture-planning-session-001.md`.

## FIAD-0003 - Canonical Ecosystem Context (fiad-build)

- Decision: create FIAD canonical context inside `harness/control/`, using
  `ecosystem/` and `projects/` as live entrypoints; legacy
  `/home/otomi/isyte-backup/Isyte/harness/memory/` remains historical source
  context only.
- Work completed: registered `FIAD-0003`, created plan, handoff, ADR,
  ecosystem docs, project profiles for CEF/HU/SAL/Harness, OpenCode adapters,
  workspace maps, session reports, implementation record, and closeout.
- Source inventory: static reads from CEF/HU/SAL/Harness code, Docker, Traefik,
  workflows, scripts, and legacy control-plane profiles; credential files,
  dumps, PDFs, XLSX, and real `.env` files were not opened.
- Security note: historical config contains placeholder/default or literal
  secret-like values; new canonical docs record variable names and owners only.
- Verification: JSON validation, internal link checks, OpenCode debug commands,
  source-path sampling, secret scan, and read-only `fiad:doctor` were run and
  recorded in the report/closeout. `fiad:doctor` passed with no blocking
  findings and no file modifications.
