# Journal - 2026-07-16

## NEXO-0038 - ChatGPT/Codex token efficiency (nexo-plan)

- Solicitud: aplicar en la app de ChatGPT el mismo ahorro de contexto y tokens
  implementado para OpenCode con Terra.
- Decision: crear un adaptador nativo de Codex y promover el compilador a una
  sola implementacion compartida bajo el control plane.
- Politica: Terra para trabajo cotidiano, `high` por defecto, `medium` para
  trabajo mecanico y `xhigh` solo para riesgo alto; verbosidad baja,
  compactacion a 64K y salida por herramienta limitada a 8K.
- Restricciones: no reducir pruebas/QA/security, no cambiar configuracion
  global, no hacer llamadas pagadas, commit, push ni deploy.
- Handoff: `handoffs/HOFF-2026-07-16-chatgpt-codex-token-efficient-workflow.md`.

## NEXO-0038 - Fail-closed del contexto compartido (nexo-build)

- Durante la implementacion, NEXO-0036 avanzo a su reporte `session-004` y su
  fila viva cambio; el compilador rechazo el paquete anterior por conflicto.
- Se refresco `state/focus.json` con la nueva evidencia y siguiente accion de
  reproduccion autenticada. No se modifico el producto ni el trabajo paralelo.

## NEXO-0038 - Implementacion y cierre (nexo-build / nexo-qa / nexo-security)

- Codex ahora usa `gpt-5.6-terra`, razonamiento `high`, verbosidad baja,
  compactacion a 64K y limite de 8K por salida de herramienta.
- El compilador vive en `harness/control/scripts/`; los adaptadores de Codex y
  OpenCode generan paquetes locales identicos de 4,148 caracteres (~1,037
  tokens).
- El arranque combinado con `AGENTS.md` se estima en ~2,017 tokens, 83.5% menos
  que el baseline completo de ~12,200 tokens.
- Verificacion: 21/21 pruebas, Codex Doctor estricto 17 OK/0 fail, Terra y
  esfuerzos requeridos disponibles. QA pass; security approved.
- No hubo llamada de modelo, cambio global, commit, push, deploy, producto, DB,
  Azure ni secretos. NEXO-0038 queda cerrado.
