# 2026-07-29

## NEXO-0053 Registered — Instagram Giveaway Follow Verification

- Registered `NEXO-0053` at P1 as a new controlled task.
- Created plan: `plans/NEXO-0053-instagram-giveaway-follow-verification.md`
- Created manifest: `state/tasks/NEXO-0053.json` (QA + Security required)
- Links to handoff: `handoffs/HOFF-2026-07-29-instagram-giveaway-dashboard-phase-4.md`
- Scope: Chrome DevTools follower extraction, automated follow verification for
  commenters and tagged accounts, `+3` / `NO_FOLLOW` eligibility flags,
  provenance metadata, and a `POST /giveaways/:id/verify-follows` endpoint.

### Prerequisites before implementation

- Fix `totalTickets` recalculation after bonus toggle.
- Fix list `totalTickets` aggregation (currently zero).
- Make sync idempotent and non-destructive.
- Add provenance fields (`verifiedAt`, `verifiedBy`, `verificationMethod`).
