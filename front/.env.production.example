# Frontend production environment placeholders (Vercel build).
# Browser traffic stays same-origin and Vercel proxies /api/v1 to ngrok.
VITE_API_BASE_URL=/api/v1

# Configure BACKEND_ORIGIN in Vercel, not in browser-exposed VITE_* variables.
# Example: https://abcd-12-34-56-78.ngrok-free.app
