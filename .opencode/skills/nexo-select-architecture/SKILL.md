---
name: nexo-select-architecture
description: Select Nexo architecture, module boundaries, integration seams, storage models, deployment topology, or design patterns. Use when a task changes a durable technical boundary or requires architectureDecision evidence; skip local reversible implementation choices.
---

# Nexo Select Architecture

This is the OpenCode adapter for
`harness/control/skills/nexo-select-architecture.md`.

Before evaluating a decision:

1. Read `AGENTS.md` and the validated session context.
2. Read the canonical skill and the task's plan, requirements, and current
   architecture evidence.
3. Use
   `harness/control/templates/architecture-decision-evaluation.md` for governed
   evidence.

Follow the canonical triggers, skip conditions, option comparison, decision
outcomes, and approval boundaries. Keep `harness/control/` authoritative; do
not redefine the procedure in this adapter.
